// 노드에서는 목적을 갖는 프로젝트 == 패키지

// 패키지 만들기 & 초기화
// npm init
// -> package.json 파일이 만들어짐

// index.js을 노드로 실행시키기
// node index.js(파일, 경로)
// ex) node index1.js, node src/index.js

// node 명령을 계속 하기에 복잡해 질 수 있어서, 명령어를 매크로처럼 만들 수 있음
// package.json에서 sctipts 안에서 만듦
// npm run start(만든 명령어)